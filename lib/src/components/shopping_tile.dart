import 'package:flutter/material.dart';

class ShoppingTile extends StatelessWidget {
  const ShoppingTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
