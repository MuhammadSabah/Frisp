import 'package:flutter/material.dart';

class RecipePostDetailScreen extends StatelessWidget {
  const RecipePostDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
